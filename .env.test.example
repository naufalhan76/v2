# Test credentials for Playwright E2E suite (gitignored).
#
# Copy to `.env.test.local` and fill in. The technician account must exist in
# the same Supabase project the dev server points at, with role TECHNICIAN
# and at least one assigned order so the offline scenarios have something to
# act on.

TEST_TECHNICIAN_EMAIL=
TEST_TECHNICIAN_PASSWORD=
